# 计算机网络

## OSI 七层
  数据从物理层到应用层的传输





## TCP/UDP
### TCP
***三次握手（正式数据发送前）***
```
客户端                          服务端

SYN -------------------------->   申请建立连接

     <--------------------------  SYN + ACK
                                  同意建立连接，并确认客户端 SYN

ACK -------------------------->   确认服务端 SYN

===== 连接建立完成 =====

HTTP 请求 / IM 消息 / 文件数据  ------------>
```
目的:
> 确认双方收发能力正常

> 同步双方初始序列号 ISN

> 建立连接状态




***确保可靠性机制（数据发送过程中）***

> 机制一：序号

TCP 会给数据编号。

例如你发送：

HelloWorld

TCP 不是只看成一整块，而是把字节编号：
```
H -> seq 100
e -> seq 101
l -> seq 102
...
```
接收方就能知道：

哪些数据到了
哪些数据没到
顺序是否乱了
有没有重复数据

> 机制二：ACK 确认

接收方收到数据后，会返回确认。

客户端发送 seq=100, len=20
服务端返回 ack=120

意思是：

100 到 119 我都收到了，下次请从 120 开始发

> 机制三：超时重传

如果发送方一直收不到 ACK，就认为包可能丢了。

发送数据
等待 ACK
超时未收到
重新发送

>机制四：滑动窗口

TCP 不会发一个包等一个 ACK，这样太慢。

它允许连续发送一批数据。

可发送窗口：[1000 ~ 5000]

在这个窗口范围内的数据可以先发出去，提高传输效率。

> 机制五：流量控制

流量控制解决的是：

不要把接收方撑爆。

接收方会告诉发送方：

我的接收缓冲区还剩 4096 字节

发送方就根据这个窗口控制发送速度。

> 机制六：拥塞控制

拥塞控制解决的是：

不要把网络打爆。

如果网络出现丢包、延迟增加，TCP 会认为可能发生拥塞，于是降低发送速率。

常见机制：

慢启动
拥塞避免
快重传
快恢复

***流量控制保护接收方，拥塞控制保护整个网络***

***TCP 是字节流协议***

这是 TCP 非常关键的点。

TCP 不保留应用层消息边界。

例如客户端调用两次：

send("hello");
send("world");

服务端可能一次收到：

helloworld

也可能分三次收到：

he
llowo
rld

这就是：
```text
粘包
半包
```

所以 TCP 应用层必须自己设计消息边界。

常见方案：

固定长度
分隔符
长度字段 + 消息体
TLV：Type + Length + Value

最常见的是：字节长度 + body

00000011 + "hello world"


***四次挥手***

目的：合理的关闭数据传输通道
```
客户端                          服务端

FIN -------------------------->
我这边数据发完了，准备关闭发送方向

     <-------------------------- ACK
我知道你不发了

     <-------------------------- FIN
我这边数据也发完了，也准备关闭发送方向

ACK -------------------------->
我知道你也不发了
```
In Conclusion:🫐

四次挥手是 TCP 用来可靠关闭连接的机制。

因为 TCP 是全双工协议，客户端到服务端、服务端到客户端两个方向的数据流需要分别关闭。

主动关闭方先发送 FIN，表示自己没有数据要发了；被动关闭方回复 ACK，表示收到关闭请求；等被动关闭方的数据也发送完，再发送 FIN；

主动关闭方最后回复 ACK。主动关闭方随后进入 TIME_WAIT，等待 2MSL，目的是保证最后一个 ACK 能被对方收到，并防止旧连接的延迟报文影响新连接。




### UDP
> 报文结构

```text
UDP 头部 + UDP 数据

UDP 头部有 4 个字段：

源端口
目标端口
长度
校验和

UDP数据：

应用层数据 payload

所以结构是：

| 源端口 | 目标端口 | 长度 | 校验和 | 数据payload |
```
> 查错机制

UDP 主要通过报文头里的校验和checksum和长度来发现报文是否损坏；但是它只能看看是不是损坏了，顺序什么的管不上

***但是发现损坏之后，UDP并不会采取补救方案也不会告诉接收方损坏信息，它会选择直接丢弃整个报文!!!***

**如果需要补救只能在应用层添加功能比如：***
```text
序号 seq：判断丢包、乱序、重复
ACK：确认收到
超时重传：没收到 ACK 就重发
缓冲区：暂存乱序包
FEC：用冗余数据恢复丢包
跳过策略：实时场景中直接丢弃旧包
```

## Http/Https

### 1. HTTP 是一种“进程间通信格式”

假设客户端进程想对服务端进程说：

```text
我要查询 id=123 的用户信息
```

如果没有 HTTP，客户端可能随便发：

```text
get user 123
```

服务端不一定知道这是什么意思。

所以 HTTP 规定了一套统一格式。

---

### 2. HTTP 请求结构

一个 HTTP 请求通常分为三部分：

```text
请求行
请求头
空行
请求体
```

例如：

```http
GET /user/123 HTTP/1.1
Host: example.com
Accept: application/json
User-Agent: Chrome

```

它的结构是：

```text
GET /user/123 HTTP/1.1       ← 请求行
Host: example.com            ← 请求头
Accept: application/json     ← 请求头
User-Agent: Chrome           ← 请求头
                              ← 空行，表示头部结束
                              ← 请求体，这里没有
```

---

***请求行：说明你想干什么***

```http
GET /user/123 HTTP/1.1
```

拆开：

```text
GET：请求方法，表示获取资源
/user/123：请求路径，表示你要哪个资源
HTTP/1.1：协议版本
```

所以这句话的意思是：

> 我想用 HTTP/1.1 协议获取 `/user/123` 这个资源。

---

***请求头：补充说明***

```http
Host: example.com
Accept: application/json
User-Agent: Chrome
```

这些是元信息。

例如：

```text
Host：我要访问哪个域名
Accept：我希望你返回什么格式
User-Agent：我是哪个客户端
Content-Type：我发送的数据是什么格式
Authorization：我的身份凭证
Cookie：我的登录状态
```

---

***请求体：真正提交的数据***

GET 通常没有请求体。

POST 通常有。

例如登录请求：

```http
POST /login HTTP/1.1
Host: example.com
Content-Type: application/json
Content-Length: 42

{"username":"zhang","password":"123456"}
```

其中 body 是：

```json
{"username":"zhang","password":"123456"}
```

服务端看到这个请求，就知道：

```text
客户端要访问 /login
数据格式是 JSON
body 里是用户名和密码
```

---


> UDP和TCP对比
你的总结非常到位，尤其是关于 **`conn_fd`** 的理解，这标志着你已经从“调包侠”进化到了理解内核资源管理机制的程序员。

我们可以用你提到的 **`conn_fd`** 作为切入点，把两者的区别推向极致：

---

1. 核心差异：由谁来维护“状态”？

TCP：内核帮你“记住了”
当你执行 `connect` 或 `accept` 成功后，内核会在进程的文件描述符表中记录下这套 **五元组**（源IP、源端口、目的IP、目的端口、协议）。
* **体现：** 因为内核已经记住了对方是谁，所以你在 `send` 或 `recv` 时不需要再传入地址。
* **代价：** 每一个连接都要占用内存资源来维护序列号、窗口大小、重传定时器等状态。

UDP：内核只当“传声筒”
UDP 是**无状态**的。内核只负责开辟一个端口，然后把数据扔出去或接进来。
* **体现：** 每次 `sendto` 都要重新告诉内核：“把这个包发给那个 IP 和端口”。
* **代价：** 内核开销极低。但如果你想确认对方收到了没，或者想按顺序组装数据，**内核不帮你做，你必须自己在 C++ 应用层写逻辑。**

---

2. 编程接口的本质区别

| 操作 | TCP (面向连接) | UDP (无连接) |
| :--- | :--- | :--- |
| **准备工作** | `listen` + `accept` (服务端) / `connect` (客户端) | 仅需 `bind` (服务端) |
| **数据边界** | **无边界（流）**：像水流，多次 `send` 可能一次 `recv` 读完。 | **有边界（包）**：像发快递，一次 `sendto` 对应一次 `recvfrom`。 |
| **读写函数** | `read`/`write` 或 `recv`/`send` | 必须用 `recvfrom`/`sendto` (除非使用了特殊的 `connect` 技巧*) |
| **资源消耗** | 较高（每个连接都有缓冲区和状态机） | 极低（仅需一个网卡端口缓冲区） |

> **\*进阶知识点：** 其实 UDP 也可以调 `connect`。但这**不会**触发三次握手，只是在内核里给这个 `sockfd` 绑定一个默认目的地，之后你就可以用 `send` 代替 `sendto` 了。这在某些高频传输场景下能稍微提升性能（减少了每次检查目的地址的开销）。

>所以在C++的socket编程当中，TCP和UDP的主要区别在于TCP有connect和listen的过程，保证客户端和对应的服务端进行连接获得编号conn_fd，然后再开始在这个conn_fd上进行recv或者send；
>而UDP是没有这两个环节的，读写的时候需要指定sockaddr来获得服务端的地址IP，因此UDP只管向对应的服务端发送或者只管接收，不管有没有成功

---


















### 3. HTTP 响应结构

服务端处理完后，也按照 HTTP 格式返回。

HTTP 响应通常分为：

```text
状态行
响应头
空行
响应体
```

例如：

```http
HTTP/1.1 200 OK
Content-Type: application/json
Content-Length: 25

{"id":123,"name":"Tom"}
```

结构是：

```text
HTTP/1.1 200 OK              ← 状态行
Content-Type: application/json ← 响应头
Content-Length: 25           ← 响应头
                              ← 空行
{"id":123,"name":"Tom"}      ← 响应体
```

---

***状态行：说明结果怎么样***

```http
HTTP/1.1 200 OK
```

意思是：

```text
我用 HTTP/1.1 返回
状态码是 200
请求成功
```

常见状态码：

```text
200：成功
301/302：重定向
400：请求错误
401：未登录
403：无权限
404：资源不存在
500：服务端错误
502：网关错误
504：网关超时
```

---

***响应头：说明返回数据的信息***

```http
Content-Type: application/json
Content-Length: 25
```

意思是：

```text
我返回的是 JSON
长度是 25 字节
```

---

***响应体：真正返回的数据***

```json
{"id":123,"name":"Tom"}
```

客户端拿到后，就可以解析 JSON，显示用户信息。

---

### 4. HTTP 如何让不同进程沟通？

本质是双方约定：

```text
客户端按照 HTTP 格式发请求
服务端按照 HTTP 格式解析请求
服务端按照 HTTP 格式返回响应
客户端按照 HTTP 格式解析响应
```

流程是：

```text
客户端进程
  ↓
构造 HTTP 请求字符串
  ↓
交给 TCP 发送
  ↓
服务端进程收到 TCP 字节流
  ↓
按照 HTTP 格式解析
  ↓
执行业务逻辑
  ↓
构造 HTTP 响应
  ↓
交给 TCP 发回客户端
  ↓
客户端解析 HTTP 响应
```

也就是说：

```text
TCP 解决“送达”
HTTP 解决“语义”
```

---

### 5. 一个完整🌰

你在浏览器访问：

```text
https://example.com/user/123
```

客户端进程生成 HTTP 请求：

```http
GET /user/123 HTTP/1.1
Host: example.com
Accept: application/json

```

TCP 把这段字节发给服务端 443 端口的进程。

服务端进程解析后理解为：

```text
有人请求 /user/123
方法是 GET
希望返回 JSON
```

服务端查询数据库后返回：

```http
HTTP/1.1 200 OK
Content-Type: application/json

{"id":123,"name":"Tom"}
```

客户端解析后知道：

```text
请求成功
返回 JSON
用户 id 是 123
名字是 Tom
```

这就是 HTTP 让两个进程沟通的方式。

---

### 6. HTTPS=HTTPS+TLS加密协议

HTTPS 不是另一套完全不同的业务格式。

准确说：

```text
HTTPS = HTTP + TLS
```

HTTP 明文长这样：

```http
GET /user/123 HTTP/1.1
Host: example.com
```

如果直接通过 TCP 传，别人抓包能看见。

HTTPS 做的是：

```text
先通过 TLS 建立安全通道
再把 HTTP 请求加密后放进去传输
```

所以 HTTPS 的流程是：

```text
TCP 三次握手
  ↓
TLS 握手
  ↓
加密 HTTP 请求
  ↓
加密 HTTP 响应
```

---🐻 explain in details

```
---

> 没有 HTTPS 会发生什么？

你登录网站，HTTP 请求可能是：

```http
POST /login HTTP/1.1
Host: bank.com
Content-Type: application/json

{"username":"zhang","password":"123456"}
```

如果这是普通 HTTP，网络中间的人抓包可能直接看到：

```text
username = zhang
password = 123456
```

甚至可以把请求改成：

```json
{"username":"zhang","password":"hacked"}
```

所以 HTTP 有三个问题：

```text
1. 明文：别人能看
2. 可篡改：别人能改
3. 无认证：你不知道对方是不是真 bank.com
```

HTTPS 用 TLS 解决这三个问题。

---

> HTTPS 的整体流程

访问：

```text
https://bank.com/login
```

真实顺序是：

```text
1. TCP 三次握手
2. TLS 握手
3. 生成共同的会话密钥
4. 用会话密钥加密 HTTP 请求
5. 服务端解密后处理 HTTP
6. 服务端加密 HTTP 响应返回
```

重点是：

🔐 **TLS 握手的目标，就是让客户端和服务端安全地得到同一个“会话密钥”。**

---

> 一个简化加密例子

假设 HTTP 原文是：

```text
password=123456
```

TLS 不会直接明文发送它，而是用一个密钥加密。

假设双方通过 TLS 握手得到同一个会话密钥：

```text
key = K
```

客户端发送前：

```text
明文：password=123456
密钥：K
加密后：A8F3C91B00D7...
```

网络中间的人只能看到：

```text
A8F3C91B00D7...
```

服务端收到后，用同一个密钥 K 解密：

```text
A8F3C91B00D7... + K  →  password=123456
```

这就是 HTTPS 的加密传输。

---

> 为什么不能直接把密钥 K 发过去？

因为如果客户端直接发：

```text
key = K
```

中间人也能看到 K。

那之后所有加密都没意义。

所以 TLS 最关键的问题是：

> **客户端和服务端如何在不直接发送密钥的情况下，得到同一个密钥？**

这就是 TLS 握手要解决的问题。

---

> TLS 握手做了什么？

简化版流程：

```text
客户端                         服务端

ClientHello  ---------------->
我支持 TLS 1.3
我支持这些加密算法
这是我的随机数 client_random

        <----------------  ServerHello + 证书
                           我选择 TLS 1.3
                           我选择这个加密算法
                           这是我的随机数 server_random
                           这是我的证书

验证证书
确认你真的是 bank.com

密钥交换
双方各自计算出同一个会话密钥

之后用会话密钥加密 HTTP 数据
```

HTTPS 主要保护三件事：

| 能力   | 含义          |
| ---- | ----------- |
| 加密   | 中间人看不懂内容    |
| 完整性  | 中间人改了内容会被发现 |
| 身份认证 | 确认服务端是真的    |

所以 HTTPS 不只是“加密 HTTP”，还包括：

```text
防窃听
防篡改
防冒充
```
### 7. HTTP 和 HTTPS 的结构区别

HTTP：

```text
HTTP 报文
  ↓
TCP
  ↓
IP
```

HTTPS：

```text
HTTP 报文
  ↓
TLS 加密
  ↓
TCP
  ↓
IP
```

注意：

> **HTTPS 里面的 HTTP 结构没有变，只是 HTTP 报文被 TLS 加密了。**

***In Conclusion***🍬

> TCP 把字节可靠地送到目标进程，但 TCP 不理解这些字节的含义。HTTP 是应用层协议，它规定了请求和响应的格式，包括请求行、请求头、请求体、状态行、响应头和响应体。

> 客户端按照 HTTP 格式构造请求，服务端按照 HTTP 格式解析请求并返回响应，因此两个进程可以用统一语义进行通信。HTTPS 则是在 HTTP 和 TCP 之间加入 TLS，先建立安全通道，再加密传输 HTTP 报文。






## DNS解析

1. 你的电脑拿到 URL
   https://www.example.com/user

2. 提取域名
   www.example.com

3. DNS 解析
   www.example.com -> 93.184.216.34

4. 你的电脑拿到目标 IP 后，发起 TCP 连接 🍍has something to say
   93.184.216.34:443

5. IP 层负责把 TCP 包送到目标服务器

6. 目标服务器收到后，根据目标端口 443
   交给监听 443 的服务进程

7. 如果是 HTTPS，再进行 TLS 握手

8. 最后发送 HTTP 请求



🍍 says:

```text
www.example.com  →  93.184.216.34
```

这里 `93.184.216.34` 是服务器 IP。

如果访问 HTTPS：

```text
https://www.example.com/login
```

最终连接的是：

```text
93.184.216.34:443
```

其中：

```text
93.184.216.34：服务器 IP
443：服务器上 HTTPS 服务监听的端口
```

所以完整定位方式是：

```text
目标 IP + 目标端口
```

也就是：

```text
服务器地址 + 服务器进程入口
```

***In Conclusion***
```text
IP 地址：定位一台主机 / 网卡 / 网络接口
端口号：定位这台主机上的某个进程或服务
```
```
DNS：域名 -> IP
TCP/UDP：IP + 端口 -> 目标进程
HTTP：请求路径、方法、头部、body -> 具体业务逻辑
```

***🥑现在可以回答，用户访问一个网址请求的时候到底发生了什么?***
⭐ 面试版回答

> 当客户端访问一个 URL（例如 http://www.baidu.com）时，首先浏览器会解析
> URL，提取出域名 www.baidu.com。然后客户端通过
> DNS 协议将域名解析为目标服务器的 IP 地址。
> 端口号不是 DNS 提供的，而是由协议决定，例如 HTTP 默认使用 80 端口，HTTPS 使用 443 端口。
> 得到 IP 和端口后，客户端通过 TCP 向服务器发起连接，经过三次握手建立 TCP 连接。如果是 HTTPS，还会在 TCP 连接之上进行 TLS 握手，完成证书校验和密钥协商，建立加密通道。
> 连接建立完成后，客户端按照 HTTP 协议构造请求报文，例如请求行、请求头和请求体，并通过 TCP 发送到服务器。
> 服务器收到数据后，IP 层确认目标主机，TCP 层根据端口号将数据交给对应的服务进程。该进程按照 HTTP 协议解析请求，执行业务逻辑，并生成 HTTP 响应。如果是 HTTPS，响应会先经过 TLS 加密，再通过 TCP/IP 返回客户端。
> 客户端接收到响应后解析 HTTP 报文，根据状态码和响应体进行处理，例如渲染页面或展示数据。

> 服务端返回数据后，客户端读取的过程可以接着前面的链路理解：
```text
服务端 HTTP 响应
  ↓
TCP 分段发送
  ↓
IP 网络传输
  ↓
客户端 TCP 接收缓冲区
  ↓
客户端进程 read/recv 读取
  ↓
如果是 HTTPS，TLS 解密
  ↓
HTTP 解析响应
  ↓
业务层使用响应数据

```

## C++ Socket 编程


下面是一套完整的 **TCP Echo Server / Client**，包含：

```text
客户端 connect 建立连接
服务端 bind/listen/accept 接收连接
服务端 recv 读取数据
服务端 send 返回数据
客户端 recv 读取返回结果
```

---

> 1. 服务端 server.cpp

```cpp
#include <arpa/inet.h>
#include <unistd.h>

#include <cstring>
#include <iostream>
#include <string>

bool send_all(int fd, const char* data, size_t len) {
    size_t sent = 0;

    while (sent < len) {
        ssize_t n = send(fd, data + sent, len - sent, 0);
        if (n <= 0) {
            return false;
        }
        sent += n;
    }

    return true;
}

int main() {
    // 1. 创建监听 socket
    int listen_fd = socket(AF_INET, SOCK_STREAM, 0);
    if (listen_fd < 0) {
        perror("socket");
        return 1;
    }

    // 允许端口快速复用，避免重启时报 Address already in use
    int opt = 1;
    setsockopt(listen_fd, SOL_SOCKET, SO_REUSEADDR, &opt, sizeof(opt));

    // 2. 设置服务端本地监听地址
    sockaddr_in server_addr{};
    server_addr.sin_family = AF_INET;                 // IPv4
    server_addr.sin_port = htons(8080);               // 监听 8080 端口
    server_addr.sin_addr.s_addr = htonl(INADDR_ANY);  // 监听本机所有网卡

    // 3. bind：把 listen_fd 绑定到本机 IP:端口
    if (bind(listen_fd, (sockaddr*)&server_addr, sizeof(server_addr)) < 0) {
        perror("bind");
        close(listen_fd);
        return 1;
    }

    // 4. listen：进入监听状态
    if (listen(listen_fd, SOMAXCONN) < 0) {
        perror("listen");
        close(listen_fd);
        return 1;
    }

    std::cout << "Server listening on 0.0.0.0:8080..." << std::endl;

    while (true) {
        // 5. accept：从已完成连接队列中取出一个客户端连接
        sockaddr_in client_addr{};
        socklen_t client_len = sizeof(client_addr);

        int conn_fd = accept(listen_fd, (sockaddr*)&client_addr, &client_len);
        if (conn_fd < 0) {
            perror("accept");
            continue;
        }

        // 打印客户端 IP 和端口
        char client_ip[INET_ADDRSTRLEN];
        inet_ntop(AF_INET, &client_addr.sin_addr, client_ip, sizeof(client_ip));

        std::cout << "Client connected: "
                  << client_ip << ":"
                  << ntohs(client_addr.sin_port)
                  << ", conn_fd = " << conn_fd
                  << std::endl;

        // 6. 使用 conn_fd 和该客户端通信
        char buf[1024];

        while (true) {
            ssize_t n = recv(conn_fd, buf, sizeof(buf), 0);

            if (n > 0) {
                std::string msg(buf, n);
                std::cout << "Received: " << msg << std::endl;

                // Echo：把收到的数据原样返回给客户端
                if (!send_all(conn_fd, buf, n)) {
                    std::cerr << "send failed" << std::endl;
                    break;
                }
            } else if (n == 0) {
                std::cout << "Client closed connection." << std::endl;
                break;
            } else {
                perror("recv");
                break;
            }
        }

        // 7. 关闭当前客户端连接
        close(conn_fd);
        std::cout << "Connection closed." << std::endl;
    }

    close(listen_fd);
    return 0;
}
```

---

> 2. 客户端 client.cpp

```cpp
#include <arpa/inet.h>
#include <unistd.h>

#include <cstring>
#include <iostream>
#include <string>

bool send_all(int fd, const char* data, size_t len) {
    size_t sent = 0;

    while (sent < len) {
        ssize_t n = send(fd, data + sent, len - sent, 0);
        if (n <= 0) {
            return false;
        }
        sent += n;
    }

    return true;
}

int main() {
    // 1. 创建客户端 socket
    int sockfd = socket(AF_INET, SOCK_STREAM, 0);
    if (sockfd < 0) {
        perror("socket");
        return 1;
    }

    // 2. 设置目标服务器地址
    sockaddr_in serv_addr{};
    serv_addr.sin_family = AF_INET;
    serv_addr.sin_port = htons(8080);

    if (inet_pton(AF_INET, "127.0.0.1", &serv_addr.sin_addr) <= 0) {
        perror("inet_pton");
        close(sockfd);
        return 1;
    }

    // 3. connect：向服务器发起 TCP 连接
    if (connect(sockfd, (sockaddr*)&serv_addr, sizeof(serv_addr)) < 0) {
        perror("connect");
        close(sockfd);
        return 1;
    }

    std::cout << "Connected to server 127.0.0.1:8080" << std::endl;

    std::string line;
    char buf[1024];

    while (true) {
        std::cout << "Input: ";
        if (!std::getline(std::cin, line)) {
            break;
        }

        if (line == "quit") {
            break;
        }

        // 4. send：向服务器发送数据
        if (!send_all(sockfd, line.data(), line.size())) {
            std::cerr << "send failed" << std::endl;
            break;
        }

        // 5. recv：接收服务器返回的数据
        ssize_t n = recv(sockfd, buf, sizeof(buf), 0);

        if (n > 0) {
            std::cout << "Echo from server: "
                      << std::string(buf, n)
                      << std::endl;
        } else if (n == 0) {
            std::cout << "Server closed connection." << std::endl;
            break;
        } else {
            perror("recv");
            break;
        }
    }

    // 6. close：关闭连接
    close(sockfd);
    return 0;
}
```


> Step1: 客户端和服务端建立连接

socket只是一个结点，在客户端和服务端***各自创建socket***，客户端的socketfd和目标服务器的ip端口的serv_addr一起connect表示连接建立

> Step2:服务端开启监听（客户端是没有监听的）

客户端和服务端建立连接之后，开启listen_fd的socket结点监听这个服务器的端口，然后这个服务器端口和其交流所有客户端IP都会进入一个队列，

通过 bind() 把 listen_fd 绑定到这个需要监听的本机服务器的地址上，一般就是客户端连接的这个本机服务器

🐈‍⬛ ***reminds you!!!目标服务器的ip端口的serv_addr和与listen_fd结点绑定监听的本机服务器的地址不是完全一样的!!!***

可以这么理解：客户端的端口绑定是一对一的，而服务端的socket绑定可以是一对多的。服务端绑定的8080里面会有很多结尾是8080的不同的客户端端口

客户端连接是一对一

客户端一个 `sockfd` 调用 `connect()` 后，通常对应一个远端：

```text
客户端 sockfd  ->  服务端 IP:端口
```

例如：

```text
客户端 192.168.1.10:50001  ->  服务端 192.168.1.100:8080
```

这个连接由四元组唯一标识：

```text
客户端 IP
客户端端口
服务端 IP
服务端端口
```

---

服务端监听是一对多

服务端一个 `listen_fd` 可以绑定一个服务入口：

```text
0.0.0.0:8080
```

然后多个客户端都可以连接它：

```text
客户端 A:50001 -> 服务端:8080
客户端 B:50002 -> 服务端:8080
客户端 C:50003 -> 服务端:8080
```

服务端每次 `accept()` 返回一个新的 `conn_fd`：

```text
listen_fd：继续监听 8080
conn_fd_A：和客户端 A 通信
conn_fd_B：和客户端 B 通信
conn_fd_C：和客户端 C 通信
```

所以更准确地说：
**客户端的一个连接 socket 通常对应一个服务端地址；服务端的一个监听 socket 可以接受多个客户端连接，每个连接由 `accept()` 返回独立的 `conn_fd`。**

然后新建conn_fd去accept从这些队列里面取出想收集的某个地址端口为client_addr客户端的通信流，这个通信流的数据现在进入了conn_fd的TCP缓存空间，最后使用recv获取这个conn-fd通信流。


> Step3 获取有效信息

开启listen_fd的socket结点监听这个服务器的端口，然后这个服务器端口和其交流所有客户端IP都会进入一个队列，然后新建conn_fd去accept从这些队列里面取出想收集的某个地址端口为client_addr客户端的通信流，这个通信流的数据现在进入了conn_fd的TCP缓存空间，最后使用recv获取这个conn-fd通信流

***🐦‍⬛Attention🐦‍⬛***
client_addr 是一个输出参数，由 accept() 填充。你只是提前准备一个空结构体，让内核把“这次 accept 到的客户端 IP 和端口”写进去
这里：
```text
client_addr{}：空的，用来接收客户端地址信息
accept()：从已完成连接队列里取出一个连接
client_addr：被内核填入这个客户端的 IP 和端口
conn_fd：这个客户端连接对应的通信 fd
```
你不能通过提前设置 client_addr 来指定“我要 accept 哪个客户端”。

accept() 通常是：

从 listen_fd 的已完成连接队列中取出一个连接
一般按队列顺序取

如果你想知道取到的是哪个客户端，再打印 client_addr：

char ip[INET_ADDRSTRLEN];

inet_ntop(AF_INET, &client_addr.sin_addr, ip, sizeof(ip));

std::cout << "client ip = " << ip
          << ", client port = "
          << ntohs(client_addr.sin_port)
          << std::endl;

如果你想“只处理某个客户端”，做法是：
```
int conn_fd = accept(...);

if (客户端 IP/端口 是我想要的) {
    recv(conn_fd, ...);
} else {
    close(conn_fd);
}
```
client_addr 不是用来筛选客户端的，而是用来接收 accept() 返回的那个客户端的地址信息。真正通信靠 conn_fd。

所以实际上conn_fd起到的作用就是从listen_fd监听的所有端口中取出一个客户端->服务端线路，这个线路对应的客户端地址是client_addr


对，`conn_fd` 的作用你理解对了：

> `conn_fd` 表示服务端从 `listen_fd` 的连接队列中取出的某一条“客户端 ↔ 服务端”的 TCP 连接。

但有一点要改：

> `conn_fd` 不是从“所有端口”里取，而是从**这个 `listen_fd` 监听的某个端口对应的连接队列**里取。

---

## 为什么 `conn_fd` 是 int？

因为在 Linux/Unix 里，socket 本质上也被抽象成**文件描述符 fd**。

文件描述符就是一个整数。

例如：

```text
0：标准输入
1：标准输出
2：标准错误
3：listen_fd
4：conn_fd_A
5：conn_fd_B
6：conn_fd_C
```

所以：

```cpp
int conn_fd = accept(listen_fd, ...);
```

这里的 `4`、`5`、`6` 不是端口号，而是：

> 当前进程中用来引用某个 socket 连接的编号。

---

## 每次 accept 的 conn_fd 会不同吗？

通常会不同。

例如：

```cpp
int conn_fd1 = accept(listen_fd, ...); // 返回 4
int conn_fd2 = accept(listen_fd, ...); // 返回 5
int conn_fd3 = accept(listen_fd, ...); // 返回 6
```

它们分别代表不同客户端连接：

```text
conn_fd=4：
客户端 A 192.168.1.10:50001 -> 服务端 192.168.1.100:8080

conn_fd=5：
客户端 B 192.168.1.11:50002 -> 服务端 192.168.1.100:8080

conn_fd=6：
客户端 C 192.168.1.12:50003 -> 服务端 192.168.1.100:8080
```

之后你用：

```cpp
recv(4, ...)
```

就是读客户端 A 的数据。

```cpp
recv(5, ...)
```

就是读客户端 B 的数据。

```cpp
recv(6, ...)
```

就是读客户端 C 的数据。

---

## fd 会复用吗？

会。

如果你关闭了：

```cpp
close(conn_fd1); // 关闭 fd=4
```

下次再有新连接：

```cpp
int conn_fd4 = accept(listen_fd, ...);
```

它可能又返回：

```text
4
```

因为 fd=4 已经空出来了。

所以更准确地说：

> 同一时刻，不同打开的连接 fd 不会相同；但关闭之后，fd 编号可以被复用。

---

## 最关键的理解

`conn_fd` 这个 int 本身不是连接数据。

它只是一个“句柄”：

```text
conn_fd = 4
```

相当于告诉内核：

> 我要操作编号为 4 的这个 TCP 连接。

真正的连接状态、接收缓冲区、发送缓冲区、四元组信息，都在内核里。

你的用户态程序只拿到一个整数 fd。

所以：

```cpp
recv(conn_fd, buf, sizeof(buf), 0);
```

意思是：

> 请内核从 `conn_fd` 指向的那个 TCP 连接的接收缓冲区里，把数据拷贝到 `buf`。

一句话总结：

> **`conn_fd` 是内核返回给应用程序的连接句柄。每个已建立的 TCP 连接都会对应一个 socket fd，fd 是 int；你通过这个 int 告诉内核要读写哪条连接。**



---

> 3. 编译运行

```bash
g++ server.cpp -o server
g++ client.cpp -o client
```

先启动服务端：

```bash
./server
```

再启动客户端：

```bash
./client
```

输入：

```text
hello
```

你会看到：

```text
Echo from server: hello
```

---

> 4. 核心流程总结

服务端：

```text
socket()
  创建 listen_fd

bind()
  绑定本机 IP:端口

listen()
  开始监听连接

accept()
  返回 conn_fd

recv(conn_fd)
  从该客户端连接读取数据

send(conn_fd)
  返回数据给该客户端

close(conn_fd)
  关闭该客户端连接
```

客户端：

```text
socket()
  创建 sockfd

connect(sockfd, serv_addr)
  连接服务器 IP:端口

send(sockfd)
  发送数据

recv(sockfd)
  接收服务端返回

close(sockfd)
  关闭连接
```


> ***C++ Socket编程总结***：
  fd是一个标识符，socket编程中的很多操作比如socket(),bind(),accept()实际上返回的是一个fd标识符(int);

  这个标识符对应着进程中的文件描述符表中的一个进程，操作系统实际上是根据这个标识符取找进程中的缓存或者数据进行操作;

  fd 是用户态程序操作内核对象的编号；socket 真正的连接状态和缓冲区在内核里，C++ 程序通过 fd 让内核帮自己读写网络数据;

 🐉 gives you examples:
```text
fd = 用户态的整数标识符
文件描述符表 = 当前进程维护的一张表
fd 表项 = 指向内核对象
内核对象 = socket / 文件 / 管道等
```

例如：

int listen_fd = socket(AF_INET, SOCK_STREAM, 0);

可能得到：

listen_fd = 3

此时：
```
当前进程 fd 表：
0 -> stdin
1 -> stdout
2 -> stderr
3 -> 内核中的监听 socket 对象
```
再执行：

int conn_fd = accept(listen_fd, ...);

可能得到：

conn_fd = 4

此时：

当前进程 fd 表：
```
0 -> stdin
1 -> stdout
2 -> stderr
3 -> 监听 socket 对象
4 -> 已建立连接 socket 对象
```
然后：

recv(conn_fd, buf, sizeof(buf), 0);

本质是：
```text
1. 你把 conn_fd=4 交给内核
2. 内核查当前进程 fd 表
3. 找到 fd=4 对应的 socket 对象
4. 从该 socket 的接收缓冲区拷贝数据到 buf
```


## I/O 流控制


> **I/O 模型解决的是：当程序面对很多 socket fd 时，如何知道哪个 fd 可以读、哪个 fd 可以写，以及如何高效处理它们。**

---

> 1. 为什么需要 I/O 模型？

前面你写的 echo server 是这样的：

```cpp
int conn_fd = accept(listen_fd, ...);

while (true) {
    recv(conn_fd, buf, sizeof(buf), 0);
    send(conn_fd, buf, n, 0);
}
```

这个版本只能很好处理**一个客户端**。

如果有很多客户端：

```text
conn_fd_1
conn_fd_2
conn_fd_3
...
conn_fd_10000
```

你不能简单地对每个 fd 都阻塞 `recv()`，因为：

```cpp
recv(conn_fd_1, ...); // 如果客户端1没发数据，程序卡住
recv(conn_fd_2, ...); // 根本执行不到
```

所以问题变成：

**我如何同时管理大量 socket fd，并且只处理已经就绪的 fd？**

这就是 I/O 多路复用。

---

> 2. 阻塞 I/O

默认 socket 是阻塞的。

```cpp
recv(fd, buf, sizeof(buf), 0);
```

如果没有数据，程序会卡住。

流程：

```text
应用调用 recv
  ↓
内核发现没有数据
  ↓
应用线程睡眠
  ↓
数据到达
  ↓
内核唤醒线程
  ↓
recv 返回
```

特点：

```text
简单
但一个线程容易被一个 fd 卡住
```

适合：

```text
单连接
简单 demo
一个连接一个线程
```

---

> 3. 非阻塞 I/O

可以把 fd 设置成非阻塞：

```cpp
fcntl(fd, F_SETFL, O_NONBLOCK);
```

此时：

```cpp
recv(fd, buf, sizeof(buf), 0);
```

如果没有数据，不会卡住，而是立即返回：

```text
-1
errno = EAGAIN 或 EWOULDBLOCK
```

特点：

```text
不会阻塞线程
但如果一直轮询，会浪费 CPU
```

伪代码：

```cpp
while (true) {
    int n = recv(fd, buf, sizeof(buf), 0);
    if (n > 0) {
        // 处理数据
    } else if (errno == EAGAIN) {
        // 没数据，下次再试
    }
}
```

问题：

```text
一直空转，CPU 忙等
```

所以非阻塞 I/O 通常要配合：

```text
select / poll / epoll
```

---

> 4. I/O 多路复用=> select/poll/epoll

I/O 多路复用的思想是：

> **让内核帮你同时监控多个 fd，只有某些 fd 就绪时，程序才被唤醒处理。**

也就是说：

```text
不是你自己挨个 recv 问有没有数据
而是你把 fd 列表交给内核
内核告诉你哪些 fd 可读/可写
```

典型 API：

```text
select
poll
epoll
```

---

:accessibility:***select**

`select` 的思想：

```text
把一组 fd 交给内核
内核检查哪些 fd 就绪
返回后应用遍历 fd 集合
```

缺点：

```text
1. fd 数量有限，通常受 FD_SETSIZE 限制
2. 每次调用都要把 fd 集合从用户态拷贝到内核态
3. 返回后还要遍历所有 fd 找就绪的
```

所以：

```text
连接数多时性能不好
```

---

:accessibility:***poll***

`poll` 改进了 select 的 fd 数量限制。

使用数组：

```cpp
struct pollfd fds[];
```

相比 select：

```text
没有固定 FD_SETSIZE 限制
```

但仍然有两个问题：

```text
每次调用仍然要传入整个 fd 数组
返回后仍然要遍历所有 fd
```

所以：

```text
大量连接时仍然效率不高
```

---

:accessibility:***epoll***

`epoll` 是 Linux 下高性能 I/O 多路复用机制。

核心思想：

> **先把要监听的 fd 注册到内核，之后内核只返回已经就绪的 fd。**

epoll 三个核心 API：

```cpp
epoll_create1()
epoll_ctl()
epoll_wait()
```



> epoll 和 select/poll 的核心区别


| 对比    | select/poll       | epoll    |
| ----- | ----------------- | -------- |
| fd 管理 | 每次调用都传整个集合        | 先注册，内核维护 |
| 返回结果  | 需要遍历所有 fd         | 只返回就绪 fd |
| 大量连接  | 效率较低              | 更适合高并发   |
| 平台    | select/poll 跨平台较好 | Linux 专有 |
| 典型用途  | 小规模连接             | 高并发网络服务  |



🐍 tells you the difference bewteen `poll` & `epoll`


> 1. poll 的工作方式

假设你有 10000 个连接 fd：

fd1, fd2, fd3, ... fd10000

每次调用：

poll(fds, 10000, -1);

都要做：

1. 应用把 10000 个 fd 数组传给内核
2. 内核遍历这 10000 个 fd，检查谁可读/可写
3. poll 返回
4. 应用再遍历这 10000 个 fd，看 revents 谁被设置了

所以即使只有 1 个 fd 有数据，也可能要：

内核扫 10000 个
应用再扫 10000 个

本质是：

poll 每次都是全量检查
> 2. epoll 的工作方式

epoll 分两步。

第一步：注册 fd，只做一次
epoll_ctl(epfd, EPOLL_CTL_ADD, fd1, &ev);
epoll_ctl(epfd, EPOLL_CTL_ADD, fd2, &ev);
epoll_ctl(epfd, EPOLL_CTL_ADD, fd3, &ev);

意思是：

内核，你以后帮我长期盯着这些 fd

这些 fd 会被维护在内核的 epoll 实例里。

第二步：等待就绪事件
int n = epoll_wait(epfd, events, MAX_EVENTS, -1);

如果只有：

fd537 可读

那么 epoll_wait 直接返回：

events[0].data.fd = fd537
n = 1

应用只处理：

recv(fd537, ...);

不用再遍历 10000 个 fd。

本质是：

epoll 返回的是“已经就绪的 fd 列表”


🐍 in conclusion!!!

***poll的工作方式是每次应用层都要把所有的fd传入内核，内核遍历这所有的fd找哪些返回有效数据再进行处理***

***而epoll的工作方式是这所有的fd都先注册到内核，内核一直同时监听所有fd，有动静的fd返回给应用层，因此效率更高***

In details:

>poll
```
每次 poll() 调用：
应用层把所有 fd 数组传给内核
内核遍历所有 fd，检查谁就绪
poll 返回
应用层再遍历所有 fd，找到 revents 被设置的 fd
然后 recv/send 处理
```
所以 poll 的特点是：
```
每次全量提交
每次全量扫描
返回后应用还要全量遍历
```
> epoll
```
先用 epoll_ctl 把 fd 注册到内核
内核长期维护这些 fd 的监听关系
有 fd 就绪时，内核把它放入就绪队列
epoll_wait 只返回就绪的 fd
应用只处理这些就绪 fd
```
所以 epoll 的特点是：
```
一次注册
长期维护
事件驱动
只返回活跃 fd
```

> 🐖一个epoll的完整使用🌰（开始监听，注册，返回，数据处理）
下面是**把 `listen_fd` 注册到 epoll** 的最小完整代码片段。

```cpp
#include <arpa/inet.h>
#include <sys/epoll.h>
#include <unistd.h>

#include <cstring>
#include <iostream>

int main() {
    // 1. 创建监听 socket
    int listen_fd = socket(AF_INET, SOCK_STREAM, 0);
    if (listen_fd < 0) {
        perror("socket");
        return 1;
    }

    // 2. 设置端口复用
    int opt = 1;
    setsockopt(listen_fd, SOL_SOCKET, SO_REUSEADDR, &opt, sizeof(opt));

    // 3. 准备服务端监听地址
    sockaddr_in server_addr{};
    server_addr.sin_family = AF_INET;
    server_addr.sin_port = htons(8080);
    server_addr.sin_addr.s_addr = htonl(INADDR_ANY);

    // 4. 绑定 listen_fd 到本机 0.0.0.0:8080
    if (bind(listen_fd, (sockaddr*)&server_addr, sizeof(server_addr)) < 0) {
        perror("bind");
        close(listen_fd);
        return 1;
    }

    // 5. 让 listen_fd 进入监听状态
    if (listen(listen_fd, SOMAXCONN) < 0) {
        perror("listen");
        close(listen_fd);
        return 1;
    }

    // 6. 创建 epoll 实例
    int epfd = epoll_create1(0);
    if (epfd < 0) {
        perror("epoll_create1");
        close(listen_fd);
        return 1;
    }

    // 7. 创建 epoll_event，描述要监听什么事件
    epoll_event ev{};
    ev.events = EPOLLIN;        // 监听“可读事件”
    ev.data.fd = listen_fd;     // 事件发生时返回 listen_fd

    // 8. 把 listen_fd 注册到 epoll
    if (epoll_ctl(epfd, EPOLL_CTL_ADD, listen_fd, &ev) < 0) {
        perror("epoll_ctl");
        close(epfd);
        close(listen_fd);
        return 1;
    }

    std::cout << "listen_fd has been registered to epoll." << std::endl;
    std::cout << "server listening on port 8080..." << std::endl;

    // 9. 等待 epoll 事件
    epoll_event events[1024];

    while (true) {
        int nready = epoll_wait(epfd, events, 1024, -1);
        if (nready < 0) {
            perror("epoll_wait");
            break;
        }

        for (int i = 0; i < nready; i++) {
            int fd = events[i].data.fd;

            if (fd == listen_fd) {
                sockaddr_in client_addr{};
                socklen_t client_len = sizeof(client_addr);

                int conn_fd = accept(
                    listen_fd,
                    (sockaddr*)&client_addr,
                    &client_len
                );

                if (conn_fd < 0) {
                    perror("accept");
                    continue;
                }

                std::cout << "new client connected, conn_fd = "
                          << conn_fd << std::endl;

                close(conn_fd);
            }
        }
    }

    close(epfd);
    close(listen_fd);
    return 0;
}
```

---

***核心注册代码是这几行***

```cpp
int epfd = epoll_create1(0);

epoll_event ev{};
ev.events = EPOLLIN;
ev.data.fd = listen_fd;

epoll_ctl(epfd, EPOLL_CTL_ADD, listen_fd, &ev);
```

---

1. `epoll_create1(0)`

```cpp
int epfd = epoll_create1(0);
```

作用：

```text
创建一个 epoll 实例
```

你可以把 `epfd` 理解成：

```text
一个事件管理器的 fd
```

它也是一个文件描述符。

此时内核里出现了一个 epoll 对象：

```text
epfd -> epoll 内核对象
```

---

2. `epoll_event ev{}`

```cpp
epoll_event ev{};
```

这个结构体用来告诉 epoll：

```text
我要监听什么事件
事件发生后返回什么信息
```

它主要有两个重要字段：

```cpp
ev.events
ev.data
```

---

3. `ev.events = EPOLLIN`

```cpp
ev.events = EPOLLIN;
```

表示：

```text
我关心可读事件
```

对于 `listen_fd` 来说：

```text
listen_fd 可读 = 有新客户端连接可以 accept()
```

对于普通 `conn_fd` 来说：

```text
conn_fd 可读 = 有客户端数据可以 recv()
```

所以同样是 `EPOLLIN`，含义取决于 fd 类型。

---

4. `ev.data.fd = listen_fd`

```cpp
ev.data.fd = listen_fd;
```

表示：

```text
如果 listen_fd 发生事件，epoll_wait 返回时请把 listen_fd 带回来
```

后面你会这样取：

```cpp
int fd = events[i].data.fd;
```

然后判断：

```cpp
if (fd == listen_fd) {
    accept(...);
}
```

---

5. `epoll_ctl(...)`

```cpp
epoll_ctl(epfd, EPOLL_CTL_ADD, listen_fd, &ev);
```

这是“注册”的真正动作。

参数含义：

```cpp
epfd
```

表示：

```text
注册到哪个 epoll 实例
```

```cpp
EPOLL_CTL_ADD
```

表示：

```text
添加一个新的 fd
```

```cpp
listen_fd
```

表示：

```text
我要监听的 fd 是 listen_fd
```

```cpp
&ev
```

表示：

```text
监听 listen_fd 的什么事件，以及事件发生后返回什么数据
```

完整含义：

> 把 `listen_fd` 添加到 `epfd` 这个 epoll 实例中，让内核帮我监听 `listen_fd` 的 `EPOLLIN` 事件；如果事件发生，`epoll_wait` 返回时把 `listen_fd` 带回来。

---

注册之后的顺序处理：

注册完成后，程序调用：

```cpp
int nready = epoll_wait(epfd, events, 1024, -1);
```

这时线程会阻塞等待。

如果没有客户端连接：

```text
epoll_wait 一直阻塞
```

如果有客户端连接到 8080：

```text
listen_fd 变成可读
epoll_wait 返回
events[0].data.fd == listen_fd
```

然后代码执行：

```cpp
int conn_fd = accept(listen_fd, ...);
```

这时 `accept()` 才真正取出一个客户端连接。

---


***`listen()***

```cpp
listen(listen_fd, SOMAXCONN);
```

作用：

```text
让 listen_fd 具备接收 TCP 连接请求的能力
```

***`epoll_ctl()`***

```cpp
epoll_ctl(epfd, EPOLL_CTL_ADD, listen_fd, &ev);
```

作用：

```text
让 epoll 帮我监控 listen_fd 有没有事件发生
```

所以：

```text
listen 是 TCP 层面的监听
epoll_ctl 是事件层面的注册
epoll_wait 是等待事件发生
```

---

> **注册 epoll，就是用 `epoll_ctl` 告诉内核：请帮我监控这个 fd 的某类事件；以后 `epoll_wait` 只要发现事件发生，就把这个 fd 返回给我。**


## epoll 的 LT 和 ET


> LT：Level Triggered，水平触发

默认模式。

含义：

> 只要 fd 还有数据没读完，epoll_wait 就会一直通知你。

例如 socket 接收缓冲区里有 100 字节。

你只读了 50 字节。

下次 `epoll_wait()` 还会继续通知你：

```text
这个 fd 仍然可读
```

优点：

```text
安全，不容易漏事件
```

缺点：

```text
如果不读干净，会重复通知
```

---

> ET：Edge Triggered，边缘触发

需要设置：

```cpp
event.events = EPOLLIN | EPOLLET;
```

含义：

***只有状态从“不可读”变成“可读”的那一刻通知一次***

如果你没有一次性把数据读完，后面可能不会再通知。

所以 ET 模式要求：

```text
fd 必须设置为非阻塞
recv 必须循环读到 EAGAIN
```

典型写法：

```cpp
while (true) {
    ssize_t n = recv(fd, buf, sizeof(buf), 0);

    if (n > 0) {
        // 处理数据
    } else if (n == 0) {
        // 对端关闭
        close(fd);
        break;
    } else {
        if (errno == EAGAIN || errno == EWOULDBLOCK) {
            // 数据读完了
            break;
        } else {
            // 出错
            close(fd);
            break;
        }
    }
}
```

---

🐦‍⬛ ***says 为什么 ET 必须配合非阻塞***

因为 ET 模式下要循环读到“没有数据”。只有非阻塞才有轮询的机制，不断检测数据是否读完，ET只提醒一次数据变化了，后面的`是否读完`都需要用户进行检测

如果 fd 是阻塞的：

```cpp
while (true) {
    recv(fd, ...);
}
```
如果使用阻塞，没有轮询数据可能永远不会读完

下一次 `recv()` 会阻塞住，整个事件循环卡死。

所以必须非阻塞：

```text
数据读完时 recv 返回 -1 + EAGAIN
事件循环继续处理其他 fd
```

一句话：

**ET 模式要一次把数据读干净，所以必须用非阻塞 fd，读到 EAGAIN 为止。**

---

> epoll 服务端核心流程🏐

```text
socket()
bind()
listen()
设置 listen_fd 非阻塞
epoll_create1()
把 listen_fd 加入 epoll，监听 EPOLLIN
循环 epoll_wait()
如果 listen_fd 可读：
    accept 新连接
    设置 conn_fd 非阻塞
    把 conn_fd 加入 epoll
如果 conn_fd 可读：
    recv 数据
    处理业务
    send 返回
```

---


## Reactor 

Reactor 是事件驱动网络模型，底层用 epoll/select/poll 等 I/O 多路复用等待事件，事件就绪后分发给对应 handler 处理。它适合高并发连接管理，是很多网络库和服务器框架的基础。

***Reactor不是一个函数，是一个工作流程！！！***

***Reactor 模型可以理解为：用一个事件循环统一监听多个 fd，当某个 fd 就绪后，把事件分发给对应的处理逻辑***

***它不是某一个具体系统调用，而是一种**网络服务器设计模式***

---



最原始的 socket server 是：

```cpp
while (true) {
    int conn_fd = accept(listen_fd, ...);
    recv(conn_fd, ...);
    send(conn_fd, ...);
}
```

问题是：

```text
accept 会阻塞
recv 会阻塞
一个线程很难同时处理很多客户端
```

如果每个客户端开一个线程：

```text
客户端 A -> 线程 A
客户端 B -> 线程 B
客户端 C -> 线程 C
```

连接多了以后：

```text
线程太多
上下文切换成本高
内存开销大
调度复杂
```

所以 Reactor 的思想是：

***不要一个连接一个线程，而是用一个事件循环管理很多连接。***

---

> Reactor 的核心组成

Reactor 通常由三部分组成：

```text
1. Event Demultiplexer：事件多路分离器
2. Reactor：事件循环和事件分发器
3. Handler：事件处理器
```

对应到 Linux 网络编程：

| Reactor 概念          | Linux 对应                                   |
| ------------------- | ------------------------------------------ |
| Event Demultiplexer | epoll / select / poll                      |
| Reactor             | while + epoll_wait + dispatch              |
| Handler             | acceptHandler / readHandler / writeHandler |

---

> 具体流程

一个 Reactor Server 大致是：

```text
1. 创建 listen_fd
2. 把 listen_fd 注册到 epoll
3. 进入事件循环
4. epoll_wait 等待事件
5. 如果 listen_fd 可读，说明有新连接，调用 acceptHandler
6. acceptHandler 调用 accept，得到 conn_fd
7. 把 conn_fd 注册到 epoll
8. 如果 conn_fd 可读，说明客户端发数据，调用 readHandler
9. readHandler 调用 recv，处理请求
10. 如果需要返回数据，调用 send 或注册写事件
```

图：

```text
             +----------------+
             |   EventLoop    |
             | while(true)    |
             +-------+--------+
                     |
                     v
              epoll_wait()
                     |
         +-----------+-----------+
         |                       |
    listen_fd 可读          conn_fd 可读
         |                       |
         v                       v
  acceptHandler              readHandler
         |                       |
         v                       v
   accept 新连接              recv 读数据
         |                       |
         v                       v
 注册 conn_fd 到 epoll       业务处理 / send
```

---

> 一个最小 Reactor 伪代码

```cpp
while (true) {
    int n = epoll_wait(epfd, events, MAX_EVENTS, -1);

    for (int i = 0; i < n; i++) {
        int fd = events[i].data.fd;

        if (fd == listen_fd) {
            handle_accept();
        } else if (events[i].events & EPOLLIN) {
            handle_read(fd);
        } else if (events[i].events & EPOLLOUT) {
            handle_write(fd);
        }
    }
}
```

这里：

```text
epoll_wait：等待事件
handle_accept：处理新连接
handle_read：处理读事件
handle_write：处理写事件
```

这就是最小 Reactor。

---


***🥑Reactor 是模式!!!是如何组织代码，分发逻辑***

```text
epoll 是工具
Reactor 是架构
```

普通 epoll 代码可能写成一大坨：

```cpp
if (fd == listen_fd) {
    accept...
} else {
    recv...
    send...
}
```

Reactor 会把逻辑拆成：

```text
Acceptor：负责新连接
Connection：负责单个连接读写
EventLoop：负责事件循环
Channel：封装 fd 和关心的事件
```

---

> Reactor 的典型对象设计


***EventLoop**

负责事件循环：

```text
epoll_wait
事件分发
执行回调
```

***Channel***

封装一个 fd 以及它关心的事件：

```text
fd
events
read_callback
write_callback
close_callback
error_callback
```

***Acceptor***

封装 `listen_fd`：

```text
监听新连接
accept
创建 TcpConnection
```

***TcpConnection***

封装一个 `conn_fd`：

```text
读取数据
发送数据
维护连接状态
输入缓冲区
输出缓冲区
```

整体：

```text
EventLoop 管多个 Channel
Channel 对应一个 fd
fd 就绪后调用 Channel 里的回调
```

---


> Reactor高并发应用

因为它是：

```text
事件驱动
非阻塞 I/O
少量线程管理大量连接
```

例如：

```text
1 个线程 + epoll 可以管理上万个连接
```

但注意：

> Reactor 适合 I/O 密集型任务，不适合在事件循环里执行耗时 CPU 任务。

如果某个 handler 里做很慢的计算：

```cpp
handle_read(fd) {
    大量计算 5 秒;
}
```

整个 EventLoop 都会被卡住。

所以工程里常见做法：

```text
Reactor 线程负责 I/O
线程池负责耗时业务
```

---

***In Conclusion:***

> Reactor 是一种事件驱动的网络编程模型。它通过 select、poll、epoll 这类 I/O 多路复用机制监听多个 fd 的事件，当某个 fd 就绪后，事件循环会把该事件分发给对应的 handler 处理。比如监听 socket 可读时调用 accept handler 接收新连接，连接 socket 可读时调用 read handler 读取数据，可写时调用 write handler 发送数据。Reactor 的核心是 I/O 多路复用、事件循环和事件分发，适合用少量线程管理大量连接。

更高级一点：

> 在工程实现中，Reactor 通常会抽象出 EventLoop、Channel、Acceptor、TcpConnection 等组件。EventLoop 负责 epoll_wait 和事件分发，Channel 封装 fd 和事件回调，Acceptor 负责新连接，TcpConnection 负责连接读写。单线程 Reactor 简单但无法利用多核，主从 Reactor 多线程可以由主 Reactor 负责 accept，从 Reactor 负责连接 I/O，业务逻辑交给线程池处理。

---


> **Reactor = I/O 多路复用 + 事件循环 + 回调分发。**


---





## UDP + 弱网环境

---

> 一、UDP 基础模型（对比 TCP）
1. 架构
```text
UDP：
- 无连接（connectionless）
- 面向报文（message-oriented）
- 不可靠（不保证到达）
- 不保证顺序
- 不保证不重复
- 无拥塞控制 / 流量控制
```

```text
TCP：
- 面向连接
- 字节流
- 可靠、有序
- 自动重传、拥塞控制
```

---

2. Socket 编程模型

```text
TCP：
socket → connect → send/recv

UDP：
socket → sendto/recvfrom
```

核心区别：

```text
UDP 每个包都是独立的
TCP 是持续连接上的字节流
```

---

> 二、弱网环境下出现的问题

弱网（Weak Network）常见问题：

```text
1. 丢包（packet loss）
2. 乱序（out-of-order）
3. 重复包（duplicate）
4. 高延迟（latency）
5. 抖动（jitter）
6. 带宽波动
7. NAT 超时 / 连接中断
```

---

> 三、UDP 在弱网下的适应策略

🔔🔔🔔
***UDP 不提供可靠性，所有传输策略必须由应用层实现，这其实可以更好的适配不同的应用情况，比如在视频或者音频中丢包可以不重传而是直接跳过防止频繁卡顿***
***但是在文件的发送中出现乱序的情况就必须要辨别出来并重新按顺序排列发送***
***这些机制的实现都靠不同应用层的代码编写而不是在UDP的传输部分***

核心结论：

```text
TCP：协议栈保证可靠
UDP：应用层决定可靠策略
```

---

***🍦So now we can see how UDP solves the problems mentioned above on the application level !!!!🍦***

> 四、UDP下的应用层协议设计（核心）

1. 基本数据结构

典型 UDP 包结构：

```text
| type | seq | ack | timestamp | payload |
```

字段含义：

```text
type       → 消息类型（DATA / ACK / PING / PONG）
seq        → 序号（检测丢包/乱序）
ack        → 确认号
timestamp  → RTT 计算 / 超时控制
payload    → 业务数据
```

---

2. 核心机制

（1）丢包检测

```text
通过 seq 判断
```

示例：

```text
收到：1,2,4 → 丢失 3
```

---
（2）重传策略

两种模式：

```text
可靠模式：
- 丢包必须重传（文件传输）

实时模式：
- 丢包直接跳过（音视频/游戏）
```

---

（3）乱序处理

```text
可靠业务：
→ 缓存 + 重排

实时业务：
→ 丢弃旧包，仅保留最新状态
```

---

（4）超时机制

发送端维护一个发送窗口或发送表：
```
seq -> 发送时间 -> payload -> 是否已确认
```
例如：
```
seq=10, send_time=100ms, payload="hello"
```
如果超过 200ms 没收到 ACK：

重传 seq=10

伪代码：
```
send_packet(seq, data);
record[seq] = {data, now};

while (true) {
    if (now - record[seq].send_time > timeout) {
        resend(seq);
    }
}
```
实际会更复杂，需要：
```text
最大重传次数
指数退避
RTT 估算
拥塞控制
去重
乱序缓存
```
（5）***指数退避（重要）***

弱网下不能疯狂重试。

否则网络越差，你发得越猛，网络更拥塞。

指数退避策略：
```
第 1 次重试：100ms 后
第 2 次重试：200ms 后
第 3 次重试：400ms 后
第 4 次重试：800ms 后
```
也就是：

timeout = base * 2^retry_count

还可以加随机抖动：

timeout = base * 2^retry_count + random(0, 50ms)

防止大量客户端同时重试造成雪崩。

> 五、UDP 弱网优化策略

1. 心跳机制（Keepalive）

```text
定期发送 PING/PONG
检测连接是否存活
维持 NAT 映射
```

---

2. RTT 与网络质量评估

```text
RTT = now - timestamp
```

统计：

```text
延迟
丢包率
抖动
```

---

3. FEC（前向纠错）

***本质：发送额外冗余包，使用这个冗余数据恢复加上成功收到的数据通过计算来恢复那些可能丢失的数据，让接收端在少量丢包时不用重传也能恢复。***

例如发送：
```
A
B
C
FEC = A ^ B ^ C
```
如果 B 丢了，可以使用FEC+A+C计算恢复：
```
B = A ^ C ^ FEC
```
适合音视频，因为：

重传可能太慢
FEC 可以更快恢复

代价是：

增加带宽消耗

4. 抖动缓冲（Jitter Buffer）

```text
缓存一小段数据再播放
```

作用：

```text
平滑网络抖动
```

代价：

```text
增加延迟
```

---

5. 数据包大小控制

```text
避免 IP 分片
建议 < 1200 bytes
```

原因：

```text
任意分片丢失 → 整个包无效
```

---

6. NAT 保活策略

NAT 保活是为了解决一个尴尬的问题：你的 UDP 包发出去后，如果不持续“活动”，回家的路（路由器的映射条目）就会被拆掉

为了防止断联会设计心跳：

```text
客户端每 5 秒发一个 PING
服务端返回 PONG

如果连续几次没收到：

认为网络不可用
触发重连 / 切换线路 / 降级
```

心跳包可以是：

```
type = PING
seq = 100
timestamp = now
```
服务端返回：

```
type = PONG
ack = 100
timestamp = 原 timestamp
```
客户端可以计算 RTT：

```
RTT = now - timestamp
```
***定期发送小包，防止 NAT 映射失效***


---

> 六、不同业务策略对比（面试可背）

1. 实时业务（音视频 / 游戏）

```text
特点：
- 低延迟优先
- 容忍丢包

策略：
- 不重传或选择性重传
- 丢弃旧数据
- 插值/预测
- FEC
- 抖动缓冲
```

---

2. 可靠业务（文件 / 控制消息）

```text
特点：
- 必须正确
- 可容忍延迟

策略：
- seq + ack
- 超时重传
- 顺序重排
```

---

3. 混合模式（典型游戏）

```text
关键操作 → 可靠（TCP 或可靠 UDP）
状态同步 → 不可靠（UDP）
```

---

> 七、客户端弱网设计（工程重点）

## 1. 必备能力

```text
1. 超时控制
2. 重试机制
3. 指数退避
4. 心跳检测
5. 断线重连
6. 去重（避免重复请求）
7. 降级策略
8. 本地缓存
9. 日志与埋点
```

---

## 2. 请求生命周期

```text
发送请求 → 等待响应 → 超时 → 重试 → 失败 → 降级
```

---

## 3. 弱网应对策略

```text
网络差：
- 降低发送频率
- 减少数据量
- 切换服务器
- 切换协议（UDP → TCP）
```

---

>八、UDP vs TCP 弱网对比

| 维度   | TCP     | UDP   |
| ---- | ------- | ----- |
| 丢包处理 | 自动重传    | 应用层处理 |
| 顺序   | 保证      | 不保证   |
| 延迟   | 高（可能阻塞） | 低     |
| 控制力  | 低       | 高     |
| 适合场景 | 请求响应    | 实时通信  |

---

> 九、最终总结⭐

```text
UDP 本质：
只负责“尽力发送一个数据报”

弱网本质：
网络不可靠是常态

工程核心：
把可靠性、顺序、重传、降级交给应用层设计
```

最关键一句：

> **UDP 的价值不在于“更快”，而在于“应用可以自主决定在弱网下如何取舍：延迟 vs 可靠性”。**


## QUIC（运行在UDP之上）
> 结构
```
UDP Datagram
  └── QUIC Packet
        └── QUIC Frame
              └── HTTP/3 Data
```
***QUIC 是一个基于 UDP 的现代传输协议，它在 UDP 之上重新实现了类似 TCP + TLS + 多路复用的一整套能力。***

***QUIC 是基于 UDP 的现代传输协议，HTTP/3 就运行在 QUIC 之上。UDP 本身只提供最基础的数据报能力***
***而 QUIC 在 UDP 之上实现了连接管理、TLS 加密、可靠传输、丢包恢复、拥塞控制和多路复用。***
***它选择 UDP 的原因是 UDP 简单、部署性好，可以在用户态快速迭代协议能力，同时避免 TCP 在 HTTP/2 场景下的队头阻塞问题***
***并通过 connection ID 支持移动端网络切换时的连接迁移***

> 实现机制

1. 通常是一个 Client（客户端进程） 与一个 Server（服务端进程） 之间建立 一个 QUIC 连接。在这个连接内部，客户端可以根据业务需求开启 成百上千个 Stream（流）
   
在 C++ 调用 QUIC 库（如 msquic）时，可以针对不同流实现不同的逻辑

2.每个流在应用层层面对数据包的要求不一定相同（有的是需要不能乱序，有的可以接收重传，有的可以接收丢包），QUIC库会在内部平衡每个流的优先级和资源占用情况，确保数据传输的效率
比如：
```
可靠流 (Reliable Stream)：模拟 TCP。如果丢包，QUIC 库会自动重传，确保数据 100% 到达且有序。

非可靠/部分可靠流 (Unreliable/Partial)：模拟 UDP。例如视频帧，丢了就丢了，QUIC 不必去重传。

优先级控制：QUIC 库内部确实有一个“调度器”。当你同时发送指令（流 A）和视频（流 B）时，调度器会优先把 UDP 包的载荷空间留给流 A 的帧。
```

3.***帧交错在一个 UDP 包中发送***是 QUIC 解决队头阻塞 (HoL Blocking) 的物理基础。

打包阶段：QUIC 引擎查看当前所有活跃的流。它从流 A 抓 500 字节做成一个 STREAM Frame，再从流 B 抓 500 字节做成另一个 STREAM Frame。

发送阶段：这两个帧被塞进同一个 UDP 报文发出去。

拆包阶段：接收端收到 UDP 包，拆开看：

流 A 的帧损坏了 -> 丢弃，通知发送端重传流 A 的这部分。

流 B 的帧是好的 -> 立即交给 C++ 应用层回调函数


## 网页/APP连接失败时候的排查

你要按**分层链路**排查。

核心顺序：

```text
URL / 参数
  ↓
DNS
  ↓
TCP / UDP
  ↓
TLS
  ↓
HTTP
  ↓
业务层
  ↓
客户端自身问题
```

---

> 1. URL 和请求参数

先确认：

```text
URL 是否正确
协议是 http 还是 https
域名是否正确
端口是否正确
路径是否正确
请求方法 GET/POST 是否正确
请求头是否缺失
body 格式是否正确
```

典型问题：

```text
把 https 写成 http
端口写错
路径多了 /
Content-Type 不对
Authorization token 缺失
```

---

> 2. DNS 排查

问题表现：

```text
域名无法解析
请求一开始就失败
不同网络下结果不同
```

检查：

```bash
nslookup example.com
dig example.com
ping example.com
```

关注：

```text
是否解析出 IP
是否解析到错误 IP
是否 IPv6 优先导致连接失败
是否 hosts 文件污染
是否公司/校园网 DNS 劫持
是否 CDN 调度异常
```

一句话：

> DNS 失败时，TCP/TLS/HTTP 根本还没开始。

---

> 3. TCP 连接排查

如果是 HTTP/HTTPS，大多数情况下先看 TCP 是否能连上：

```bash
telnet example.com 443
nc -vz example.com 443
```

关注：

```text
connect timeout：网络不可达、防火墙、服务端没监听
connection refused：目标主机可达，但端口没服务
connection reset：连接被对端或中间设备重置
```

客户端代码里看：

```text
connect() 是否成功
errno 是什么
是否超时
是否被代理/防火墙拦截
```

---

> 4. TLS/HTTPS 排查

如果 TCP 连上，但 HTTPS 失败，看 TLS。

常见问题：

```text
证书过期
证书域名不匹配
证书链不完整
系统时间错误
根证书库过旧
自签名证书不受信任
代理抓包替换证书
TLS 版本或加密套件不兼容
```

可以用：

```bash
openssl s_client -connect example.com:443 -servername example.com
```

关注：

```text
证书 CN/SAN 是否匹配域名
verify return code 是否为 0
服务端是否支持当前 TLS 版本
```

---

> 5. HTTP 层排查

如果 TLS 成功，但接口失败，看 HTTP。

关注：

```text
状态码
响应头
响应体
重定向
缓存
Cookie
Authorization
Content-Type
Content-Length
```

常见状态：

```text
400：请求格式错
401：未登录/token 无效
403：无权限
404：路径错
429：限流
500：服务端内部错误
502/503/504：网关或上游服务异常
```

用：

```bash
curl -v https://example.com/api
```

`curl -v` 很有用，因为它能显示：

```text
DNS
TCP
TLS
HTTP 请求头
HTTP 响应头
状态码
```

---

> 6. UDP/QUIC 排查

如果客户端使用 QUIC/HTTP/3、音视频、游戏、RTC，就要看 UDP。

关注：

```text
UDP 是否被网络屏蔽
NAT 映射是否失效
丢包率
RTT
抖动
乱序
服务端是否支持 QUIC
是否 fallback 到 TCP/HTTP/2
```

排查思路：

```text
同网络下 TCP 正常但 QUIC 不通 → 可能 UDP 被拦
Wi-Fi 正常，蜂窝异常 → 运营商/NAT/弱网问题
QUIC 失败但 HTTP/2 成功 → fallback 是否正常
```

---

>7. 客户端自身问题

不要只怪网络，也要查客户端实现：

```text
是否在 UI 线程阻塞
超时时间是否太短
连接池是否耗尽
fd 是否泄漏
socket 是否忘记 close
send 是否没处理部分发送
recv 是否没处理半包
HTTPS 是否没配置 SNI
代理配置是否错误
系统时间是否错误
本地缓存是否脏
```

C++ 客户端尤其要查：

```text
errno
日志是否完整
是否 SIGPIPE
是否非阻塞 I/O 没处理 EAGAIN
epoll 是否漏事件
ET 模式是否没读到 EAGAIN
```

---


🐤In Conclusion:

> 首先检查 URL、协议、端口、请求参数是否正确；然后检查 DNS 是否解析成功以及解析 IP 是否符合预期；
> 接着检查 TCP 是否能连接目标 IP 和端口，如果是 UDP/QUIC 则检查 UDP 是否被拦截、是否丢包或 NAT 超时；
> 如果是 HTTPS，再检查 TLS 握手和证书链；
> 连接建立后看 HTTP 状态码、响应头和业务错误码。
> 最后排查客户端自身问题，比如超时设置、代理、连接池、线程阻塞、socket 泄漏、send/recv 处理不完整、epoll 事件处理异常等。
> **QUIC 是 UDP 上的现代可靠安全传输；网络失败排查要按 DNS → 传输层 → TLS → HTTP → 业务层 → 客户端实现逐层收敛。**





## HTTP 客户端工程
> HTTP 语义
```
GET / POST
URL path
Header
Body
Status Code
```
HTTP/1.1 用文本格式表达这些语义。

HTTP/2 用二进制 frame 表达这些语义。

***HTTP/1.1 和 HTTP/2 都表达 HTTP 的请求/响应语义，HTTP/1.1 和 HTTP/2 都是 HTTP 的不同版本，都用于表达客户端请求和服务端响应，但它们的编码格式和传输组织方式不同***

>HTTP/1.1对比HTTP/2.2

二者组织结构相同例如方法、路径、头部、body 和状态码。

HTTP/1.1 使用文本格式，报文直观可读，例如请求行、Header 和 Body：
```
Host: example.com
User-Agent: Chrome
Cookie: xxxxx
Accept: text/html
```
但存在头部冗余、连接并发能力有限、同一连接上响应容易受到顺序约束等问题。(顺序执行，前面的请求堵住了后面也无法执行


HTTP/2 保留 HTTP 语义，但改为二进制分帧，引入 frame 和 stream：

HTTP/2 中有几个重要概念：
```text
Connection：一个 TCP 连接
Stream：连接中的一条逻辑请求/响应通道
Frame：HTTP/2 传输的最小单位
```
结构可以理解为：
```
TCP Connection
  ├── Stream 1
  │     ├── HEADERS frame
  │     └── DATA frame
  ├── Stream 3
  │     ├── HEADERS frame
  │     └── DATA frame
  └── Stream 5
        ├── HEADERS frame
        └── DATA frame
```

🍓Frame 是 HTTP/2 传输的最小单位。

例如：
```
HEADERS frame：传请求头/响应头
DATA frame：传 body 数据
SETTINGS frame：传连接配置
PING frame：检测连接
RST_STREAM frame：重置某个 stream
```

🍓Stream 是一个逻辑上的请求-响应通道。

比如浏览器同时请求：
```
/index.html
/style.css
/app.js
/logo.png
```
HTTP/2 可以在一个 TCP 连接里创建多个 stream：
```
stream 1：/index.html
stream 3：/style.css
stream 5：/app.js
stream 7：/logo.png
```
这些 stream 的 frame 可以交错传输。

HTTP/2.2在一个 TCP 连接上支持多个 stream 多路复用，不同 stream 的 frame 可以交错发送，

同时使用 HPACK 压缩头部，减少冗余并提升并发效率

不过 HTTP/2 仍然基于 TCP，因此如果 TCP 层发生丢包，后续字节即使已经到达，也必须等待丢失字节重传后才能按序交付给 HTTP/2 层，
所以仍然存在 TCP 层队头阻塞。



## HTTPS缓存

🐇**HTTP 缓存缓存的不是“服务端地址等信息”，而是服务端返回的资源内容以及相关缓存元信息。**

比如缓存的是：

```text
logo.png 图片内容
app.js 文件内容
style.css 文件内容
某个接口响应 JSON
```

以及：

```text
Cache-Control
ETag
Last-Modified
Expires
```


> **强缓存**：客户端本地已经缓存了某个资源，并且根据 `Cache-Control: max-age` 判断缓存还没过期，那么下次访问同一资源时，不需要请求服务端，直接使用本地缓存。
> **协商缓存**：客户端本地有缓存，但强缓存过期了，于是请求服务端确认这个资源有没有变化。如果没变，服务端返回 `304 Not Modified`，客户端继续用本地缓存；如果变了，服务端返回 `200 OK` 和新的资源内容。

例子：

```http
Cache-Control: max-age=3600
ETag: "v1"
```

含义：

```text
3600 秒内：直接用本地缓存，不问服务端
3600 秒后：带 ETag 问服务端 v1 还能不能用
```

```text
强缓存：不问服务端，直接用
协商缓存：问服务端，能用就 304，不能用就 200 + 新资源
```





















## 客户端发出响应但是没有回应逐步分析

***🐦可以按**分层链路**排查。客户端“没有响应”不等于 HTTP 没响应，可能是 DNS、TCP、TLS、HTTP、业务层任意一层卡住。🐦***

核心顺序：

```text
URL/参数 → DNS → TCP/UDP → TLS → HTTP → 业务层 → 客户端实现
```

---

> 1. 先确认请求有没有真正发出去

检查：

```text
URL 是否正确
协议是 http 还是 https
域名是否正确
端口是否正确
路径是否正确
请求方法 GET/POST 是否正确
Header 是否完整
Body 格式是否正确
```

典型问题：

```text
http/https 写错
端口写错
路径写错
Content-Type 错
Authorization / Cookie 缺失
请求体 JSON 格式错误
```

HTTP 本身是无状态应用层协议，RFC 9110 定义了 HTTP 的整体语义，包括 URI、方法、状态码和请求/响应语义。([IETF Datatracker][1])

---

> 2. DNS：域名是否解析成功

如果 DNS 失败，后续 TCP/TLS/HTTP 都不会开始。

检查：

```bash
nslookup example.com
dig example.com
ping example.com🐟
```
***🎣PING是如何检测DNS解析是否成功的？？？***
```text
你输入：

ping www.example.com

实际发生：

1. 系统发现目标是域名，不是 IP
2. 查询本机 DNS 缓存 / hosts / DNS 服务器
3. 得到 IP，比如 93.184.216.34
4. ping 向这个 IP 发送 ICMP Echo Request
5. 对方如果允许，会返回 ICMP Echo Reply

所以：

ping 域名 = DNS 解析 + ICMP 连通性测试
```

```text
是否能解析出 IP
是否解析到错误 IP
是否 IPv6 优先导致失败
hosts 是否污染
公司/校园网 DNS 是否劫持
CDN 是否调度异常
```

判断：

```text
域名解析失败 → DNS 问题
解析成功但连不上 → 继续查传输层
```

---

> 3. TCP：是否成功连接 IP:端口

HTTP/1.1、HTTP/2 通常基于 TCP。TCP 是传输层协议，RFC 9293 规定了 TCP 的连接、可靠传输和状态机等行为。([IETF Datatracker][2])

检查：

```bash
nc -vz example.com 443
telnet example.com 443
```

常见现象：

```text
connect timeout
→ 网络不可达、防火墙丢包、服务端无响应

connection refused
→ 主机可达，但目标端口没有服务监听

connection reset
→ 对端或中间设备主动重置连接
```

C++ 客户端里看：

```cpp
int ret = connect(sockfd, ...);
if (ret < 0) {
    perror("connect");
}
```

重点看：

```text
errno
超时时间
代理设置
防火墙
服务端是否监听该端口
```

---

> 4. TLS：HTTPS 是否握手成功

如果 TCP 已经连上，但 HTTPS 没响应，要查 TLS。

TLS 1.3 的 RFC 8446 说明 TLS 用于让客户端/服务端应用安全通信，防止窃听、篡改和伪造。([IETF Datatracker][3])

检查：

```bash
openssl s_client -connect example.com:443 -servername example.com
```

常见问题：

```text
证书过期
证书域名不匹配
证书链不完整
系统时间错误
根证书库过旧
自签名证书不被信任
TLS 版本不兼容
加密套件不兼容
HTTPS 抓包代理替换证书
SNI 没传
```

C++ HTTPS 客户端尤其要注意：

```text
OpenSSL 是否设置 SNI
证书校验路径是否正确
系统时间是否正确
TLS 握手超时是否合理
```

---

> 5. HTTP：请求是否符合协议，响应如何判断

如果 TLS 成功，说明安全通道已经建立；接下来排查 HTTP。

排查：

```bash
curl -v https://example.com/api
```

看：

```text
请求头是否正确
状态码是多少
是否有重定向
是否有响应头
Content-Length 是否正确
Transfer-Encoding 是否为 chunked
服务端是否一直不结束响应
```

常见状态码含义来自 RFC 9110：状态码是三位数字，用于描述请求结果，范围是 100 到 599，并按首位分成 1xx、2xx、3xx、4xx、5xx。([RFC Editor][4])

常见判断：

```text
2xx：请求成功
3xx：重定向，检查 Location
4xx：客户端请求问题
5xx：服务端或网关问题
```

特别注意：

```text
没有响应 ≠ 没有 HTTP 状态码
如果已经收到 4xx/5xx，那 HTTP 层其实有响应，只是业务失败
```

---

> 6. 客户端读取是否正确

服务端返回数据后，客户端不是“一次 recv 就读完整响应”。

TCP 是字节流，所以：

```text
一次 send 不等于一次 recv
一次 recv 不一定是完整 HTTP 响应
```

客户端应当：

```text
1. 先读到 \r\n\r\n，解析响应头
2. 如果有 Content-Length，继续读满指定长度 body
3. 如果是 chunked，按 chunk 格式读到 0 chunk
4. 如果靠连接关闭结束，就读到 recv 返回 0
```

C++ 里要注意：

```text
recv 返回 0：对端关闭连接
recv 返回 -1：检查 errno
非阻塞 I/O：EAGAIN/EWOULDBLOCK 不是致命错误
send 不保证一次发完
```

---

> 7. UDP/QUIC 场景

如果请求走 UDP、QUIC、HTTP/3、RTC、游戏协议，要额外查：

```text
UDP 是否被网络屏蔽
NAT 映射是否超时
丢包率
RTT
抖动
乱序
是否 fallback 到 TCP/HTTP/2
```

现象：

```text
TCP/HTTPS 正常，QUIC 不通
→ 可能 UDP 被防火墙、运营商或公司网络拦截
```

---

## 8. 客户端自身实现问题

C++ 客户端常见坑：

```text
连接池耗尽
fd 泄漏
socket 忘记 close
阻塞在 UI 线程
超时时间设置不合理
send 没处理部分发送
recv 没处理半包
epoll ET 模式没读到 EAGAIN
SIGPIPE 导致进程退出
代理配置错误
缓存脏数据
```

---

## 面试版回答

> 我会按分层排查。首先确认 URL、协议、端口、路径、请求方法、Header 和 Body 是否正确。然后检查 DNS 是否解析成功，解析出的 IP 是否符合预期。接着检查 TCP 是否能连接目标 IP 和端口，比如是否超时、拒绝连接或被 reset。如果是 HTTPS，需要继续检查 TLS 握手，包括证书链、域名匹配、系统时间、TLS 版本、SNI 和加密套件。TLS 成功后再看 HTTP 层，检查状态码、响应头、Content-Length、Transfer-Encoding、重定向和业务错误码。最后排查客户端自身实现，比如超时设置、连接池、代理、fd 泄漏、send/recv 处理不完整、非阻塞 I/O 和 epoll 事件处理问题。

最短记忆：

```text
DNS 能否解析
IP:端口能否连接
TLS 能否握手
HTTP 是否有状态码
Body 是否读完整
业务码是否成功
客户端实现是否有 bug
```














