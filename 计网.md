# 计算机网络

## OSI 七层
  数据从物理层到应用层的传输





## TCP/UDP
### TCP
***三次握手（正式数据发送前）***
```
客户端                          服务端

SYN -------------------------->   申请建立连接

     <--------------------------  SYN + ACK
                                  同意建立连接，并确认客户端 SYN

ACK -------------------------->   确认服务端 SYN

===== 连接建立完成 =====

HTTP 请求 / IM 消息 / 文件数据  ------------>
```
目的:
> 确认双方收发能力正常

> 同步双方初始序列号 ISN

> 建立连接状态
















数据发送过程中的确保可靠性机制
四次挥手

### UDP
报文结构
应用层发现丢包后的自处理机制




## Http/Https

### 1. HTTP 是一种“进程间通信格式”

假设客户端进程想对服务端进程说：

```text
我要查询 id=123 的用户信息
```

如果没有 HTTP，客户端可能随便发：

```text
get user 123
```

服务端不一定知道这是什么意思。

所以 HTTP 规定了一套统一格式。

---

### 2. HTTP 请求结构

一个 HTTP 请求通常分为三部分：

```text
请求行
请求头
空行
请求体
```

例如：

```http
GET /user/123 HTTP/1.1
Host: example.com
Accept: application/json
User-Agent: Chrome

```

它的结构是：

```text
GET /user/123 HTTP/1.1       ← 请求行
Host: example.com            ← 请求头
Accept: application/json     ← 请求头
User-Agent: Chrome           ← 请求头
                              ← 空行，表示头部结束
                              ← 请求体，这里没有
```

---

***请求行：说明你想干什么***

```http
GET /user/123 HTTP/1.1
```

拆开：

```text
GET：请求方法，表示获取资源
/user/123：请求路径，表示你要哪个资源
HTTP/1.1：协议版本
```

所以这句话的意思是：

> 我想用 HTTP/1.1 协议获取 `/user/123` 这个资源。

---

***请求头：补充说明***

```http
Host: example.com
Accept: application/json
User-Agent: Chrome
```

这些是元信息。

例如：

```text
Host：我要访问哪个域名
Accept：我希望你返回什么格式
User-Agent：我是哪个客户端
Content-Type：我发送的数据是什么格式
Authorization：我的身份凭证
Cookie：我的登录状态
```

---

***请求体：真正提交的数据***

GET 通常没有请求体。

POST 通常有。

例如登录请求：

```http
POST /login HTTP/1.1
Host: example.com
Content-Type: application/json
Content-Length: 42

{"username":"zhang","password":"123456"}
```

其中 body 是：

```json
{"username":"zhang","password":"123456"}
```

服务端看到这个请求，就知道：

```text
客户端要访问 /login
数据格式是 JSON
body 里是用户名和密码
```

---

### 3. HTTP 响应结构

服务端处理完后，也按照 HTTP 格式返回。

HTTP 响应通常分为：

```text
状态行
响应头
空行
响应体
```

例如：

```http
HTTP/1.1 200 OK
Content-Type: application/json
Content-Length: 25

{"id":123,"name":"Tom"}
```

结构是：

```text
HTTP/1.1 200 OK              ← 状态行
Content-Type: application/json ← 响应头
Content-Length: 25           ← 响应头
                              ← 空行
{"id":123,"name":"Tom"}      ← 响应体
```

---

***状态行：说明结果怎么样***

```http
HTTP/1.1 200 OK
```

意思是：

```text
我用 HTTP/1.1 返回
状态码是 200
请求成功
```

常见状态码：

```text
200：成功
301/302：重定向
400：请求错误
401：未登录
403：无权限
404：资源不存在
500：服务端错误
502：网关错误
504：网关超时
```

---

***响应头：说明返回数据的信息***

```http
Content-Type: application/json
Content-Length: 25
```

意思是：

```text
我返回的是 JSON
长度是 25 字节
```

---

***响应体：真正返回的数据***

```json
{"id":123,"name":"Tom"}
```

客户端拿到后，就可以解析 JSON，显示用户信息。

---

### 4. HTTP 如何让不同进程沟通？

本质是双方约定：

```text
客户端按照 HTTP 格式发请求
服务端按照 HTTP 格式解析请求
服务端按照 HTTP 格式返回响应
客户端按照 HTTP 格式解析响应
```

流程是：

```text
客户端进程
  ↓
构造 HTTP 请求字符串
  ↓
交给 TCP 发送
  ↓
服务端进程收到 TCP 字节流
  ↓
按照 HTTP 格式解析
  ↓
执行业务逻辑
  ↓
构造 HTTP 响应
  ↓
交给 TCP 发回客户端
  ↓
客户端解析 HTTP 响应
```

也就是说：

```text
TCP 解决“送达”
HTTP 解决“语义”
```

---

### 5. 一个完整🌰

你在浏览器访问：

```text
https://example.com/user/123
```

客户端进程生成 HTTP 请求：

```http
GET /user/123 HTTP/1.1
Host: example.com
Accept: application/json

```

TCP 把这段字节发给服务端 443 端口的进程。

服务端进程解析后理解为：

```text
有人请求 /user/123
方法是 GET
希望返回 JSON
```

服务端查询数据库后返回：

```http
HTTP/1.1 200 OK
Content-Type: application/json

{"id":123,"name":"Tom"}
```

客户端解析后知道：

```text
请求成功
返回 JSON
用户 id 是 123
名字是 Tom
```

这就是 HTTP 让两个进程沟通的方式。

---

### 6. HTTPS=HTTPS+TLS加密协议

HTTPS 不是另一套完全不同的业务格式。

准确说：

```text
HTTPS = HTTP + TLS
```

HTTP 明文长这样：

```http
GET /user/123 HTTP/1.1
Host: example.com
```

如果直接通过 TCP 传，别人抓包能看见。

HTTPS 做的是：

```text
先通过 TLS 建立安全通道
再把 HTTP 请求加密后放进去传输
```

所以 HTTPS 的流程是：

```text
TCP 三次握手
  ↓
TLS 握手
  ↓
加密 HTTP 请求
  ↓
加密 HTTP 响应
```

---🐻 explain in details

```
---

# 1. 没有 HTTPS 会发生什么？

你登录网站，HTTP 请求可能是：

```http
POST /login HTTP/1.1
Host: bank.com
Content-Type: application/json

{"username":"zhang","password":"123456"}
```

如果这是普通 HTTP，网络中间的人抓包可能直接看到：

```text
username = zhang
password = 123456
```

甚至可以把请求改成：

```json
{"username":"zhang","password":"hacked"}
```

所以 HTTP 有三个问题：

```text
1. 明文：别人能看
2. 可篡改：别人能改
3. 无认证：你不知道对方是不是真 bank.com
```

HTTPS 用 TLS 解决这三个问题。

---

# 2. HTTPS 的整体流程

访问：

```text
https://bank.com/login
```

真实顺序是：

```text
1. TCP 三次握手
2. TLS 握手
3. 生成共同的会话密钥
4. 用会话密钥加密 HTTP 请求
5. 服务端解密后处理 HTTP
6. 服务端加密 HTTP 响应返回
```

重点是：

> **TLS 握手的目标，就是让客户端和服务端安全地得到同一个“会话密钥”。**

---

# 3. 一个简化加密例子

假设 HTTP 原文是：

```text
password=123456
```

TLS 不会直接明文发送它，而是用一个密钥加密。

假设双方通过 TLS 握手得到同一个会话密钥：

```text
key = K
```

客户端发送前：

```text
明文：password=123456
密钥：K
加密后：A8F3C91B00D7...
```

网络中间的人只能看到：

```text
A8F3C91B00D7...
```

服务端收到后，用同一个密钥 K 解密：

```text
A8F3C91B00D7... + K  →  password=123456
```

这就是 HTTPS 的加密传输。

---

# 4. 为什么不能直接把密钥 K 发过去？

因为如果客户端直接发：

```text
key = K
```

中间人也能看到 K。

那之后所有加密都没意义。

所以 TLS 最关键的问题是：

> **客户端和服务端如何在不直接发送密钥的情况下，得到同一个密钥？**

这就是 TLS 握手要解决的问题。

---

# 5. TLS 握手做了什么？

简化版流程：

```text
客户端                         服务端

ClientHello  ---------------->
我支持 TLS 1.3
我支持这些加密算法
这是我的随机数 client_random

        <----------------  ServerHello + 证书
                           我选择 TLS 1.3
                           我选择这个加密算法
                           这是我的随机数 server_random
                           这是我的证书

验证证书
确认你真的是 bank.com

密钥交换
双方各自计算出同一个会话密钥

之后用会话密钥加密 HTTP 数据
```

HTTPS 主要保护三件事：

| 能力   | 含义          |
| ---- | ----------- |
| 加密   | 中间人看不懂内容    |
| 完整性  | 中间人改了内容会被发现 |
| 身份认证 | 确认服务端是真的    |

所以 HTTPS 不只是“加密 HTTP”，还包括：

```text
防窃听
防篡改
防冒充
```

---


### 7. HTTP 和 HTTPS 的结构区别

HTTP：

```text
HTTP 报文
  ↓
TCP
  ↓
IP
```

HTTPS：

```text
HTTP 报文
  ↓
TLS 加密
  ↓
TCP
  ↓
IP
```

注意：

> **HTTPS 里面的 HTTP 结构没有变，只是 HTTP 报文被 TLS 加密了。**

***In Conclusion***🍬
> TCP 把字节可靠地送到目标进程，但 TCP 不理解这些字节的含义。HTTP 是应用层协议，它规定了请求和响应的格式，包括请求行、请求头、请求体、状态行、响应头和响应体。

> 客户端按照 HTTP 格式构造请求，服务端按照 HTTP 格式解析请求并返回响应，因此两个进程可以用统一语义进行通信。HTTPS 则是在 HTTP 和 TCP 之间加入 TLS，先建立安全通道，再加密传输 HTTP 报文。






## DNS解析

1. 你的电脑拿到 URL
   https://www.example.com/user

2. 提取域名
   www.example.com

3. DNS 解析
   www.example.com -> 93.184.216.34

4. 你的电脑拿到目标 IP 后，发起 TCP 连接 🍍has something to say
   93.184.216.34:443

5. IP 层负责把 TCP 包送到目标服务器

6. 目标服务器收到后，根据目标端口 443
   交给监听 443 的服务进程

7. 如果是 HTTPS，再进行 TLS 握手

8. 最后发送 HTTP 请求



🍍 says:

```text
www.example.com  →  93.184.216.34
```

这里 `93.184.216.34` 是服务器 IP。

如果访问 HTTPS：

```text
https://www.example.com/login
```

最终连接的是：

```text
93.184.216.34:443
```

其中：

```text
93.184.216.34：服务器 IP
443：服务器上 HTTPS 服务监听的端口
```

所以完整定位方式是：

```text
目标 IP + 目标端口
```

也就是：

```text
服务器地址 + 服务器进程入口
```

***In Conclusion***
```text
IP 地址：定位一台主机 / 网卡 / 网络接口
端口号：定位这台主机上的某个进程或服务
```
```
DNS：域名 -> IP
TCP/UDP：IP + 端口 -> 目标进程
HTTP：请求路径、方法、头部、body -> 具体业务逻辑
```
























