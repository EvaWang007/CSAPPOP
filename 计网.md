# 计算机网络

## OSI 七层
  数据从物理层到应用层的传输





## TCP/UDP
### TCP
三次握手
数据发送过程中的确保可靠性机制
四次挥手

### UDP
报文结构
应用层发现丢包后的自处理机制




## Http/Https

# 1. HTTP 是一种“进程间通信格式”

假设客户端进程想对服务端进程说：

```text
我要查询 id=123 的用户信息
```

如果没有 HTTP，客户端可能随便发：

```text
get user 123
```

服务端不一定知道这是什么意思。

所以 HTTP 规定了一套统一格式。

---

# 2. HTTP 请求结构

一个 HTTP 请求通常分为三部分：

```text
请求行
请求头
空行
请求体
```

例如：

```http
GET /user/123 HTTP/1.1
Host: example.com
Accept: application/json
User-Agent: Chrome

```

它的结构是：

```text
GET /user/123 HTTP/1.1       ← 请求行
Host: example.com            ← 请求头
Accept: application/json     ← 请求头
User-Agent: Chrome           ← 请求头
                              ← 空行，表示头部结束
                              ← 请求体，这里没有
```

---

## 请求行：说明你想干什么

```http
GET /user/123 HTTP/1.1
```

拆开：

```text
GET：请求方法，表示获取资源
/user/123：请求路径，表示你要哪个资源
HTTP/1.1：协议版本
```

所以这句话的意思是：

> 我想用 HTTP/1.1 协议获取 `/user/123` 这个资源。

---

## 请求头：补充说明

```http
Host: example.com
Accept: application/json
User-Agent: Chrome
```

这些是元信息。

例如：

```text
Host：我要访问哪个域名
Accept：我希望你返回什么格式
User-Agent：我是哪个客户端
Content-Type：我发送的数据是什么格式
Authorization：我的身份凭证
Cookie：我的登录状态
```

---

## 请求体：真正提交的数据

GET 通常没有请求体。

POST 通常有。

例如登录请求：

```http
POST /login HTTP/1.1
Host: example.com
Content-Type: application/json
Content-Length: 42

{"username":"zhang","password":"123456"}
```

其中 body 是：

```json
{"username":"zhang","password":"123456"}
```

服务端看到这个请求，就知道：

```text
客户端要访问 /login
数据格式是 JSON
body 里是用户名和密码
```

---

# 3. HTTP 响应结构

服务端处理完后，也按照 HTTP 格式返回。

HTTP 响应通常分为：

```text
状态行
响应头
空行
响应体
```

例如：

```http
HTTP/1.1 200 OK
Content-Type: application/json
Content-Length: 25

{"id":123,"name":"Tom"}
```

结构是：

```text
HTTP/1.1 200 OK              ← 状态行
Content-Type: application/json ← 响应头
Content-Length: 25           ← 响应头
                              ← 空行
{"id":123,"name":"Tom"}      ← 响应体
```

---

## 状态行：说明结果怎么样

```http
HTTP/1.1 200 OK
```

意思是：

```text
我用 HTTP/1.1 返回
状态码是 200
请求成功
```

常见状态码：

```text
200：成功
301/302：重定向
400：请求错误
401：未登录
403：无权限
404：资源不存在
500：服务端错误
502：网关错误
504：网关超时
```

---

## 响应头：说明返回数据的信息

```http
Content-Type: application/json
Content-Length: 25
```

意思是：

```text
我返回的是 JSON
长度是 25 字节
```

---

## 响应体：真正返回的数据

```json
{"id":123,"name":"Tom"}
```

客户端拿到后，就可以解析 JSON，显示用户信息。

---

# 4. HTTP 如何让不同进程沟通？

本质是双方约定：

```text
客户端按照 HTTP 格式发请求
服务端按照 HTTP 格式解析请求
服务端按照 HTTP 格式返回响应
客户端按照 HTTP 格式解析响应
```

流程是：

```text
客户端进程
  ↓
构造 HTTP 请求字符串
  ↓
交给 TCP 发送
  ↓
服务端进程收到 TCP 字节流
  ↓
按照 HTTP 格式解析
  ↓
执行业务逻辑
  ↓
构造 HTTP 响应
  ↓
交给 TCP 发回客户端
  ↓
客户端解析 HTTP 响应
```

也就是说：

```text
TCP 解决“送达”
HTTP 解决“语义”
```

---

# 5. 一个完整例子

你在浏览器访问：

```text
https://example.com/user/123
```

客户端进程生成 HTTP 请求：

```http
GET /user/123 HTTP/1.1
Host: example.com
Accept: application/json

```

TCP 把这段字节发给服务端 443 端口的进程。

服务端进程解析后理解为：

```text
有人请求 /user/123
方法是 GET
希望返回 JSON
```

服务端查询数据库后返回：

```http
HTTP/1.1 200 OK
Content-Type: application/json

{"id":123,"name":"Tom"}
```

客户端解析后知道：

```text
请求成功
返回 JSON
用户 id 是 123
名字是 Tom
```

这就是 HTTP 让两个进程沟通的方式。

---

# 6. HTTPS 又是什么？

HTTPS 不是另一套完全不同的业务格式。

准确说：

```text
HTTPS = HTTP + TLS
```

HTTP 明文长这样：

```http
GET /user/123 HTTP/1.1
Host: example.com
```

如果直接通过 TCP 传，别人抓包能看见。

HTTPS 做的是：

```text
先通过 TLS 建立安全通道
再把 HTTP 请求加密后放进去传输
```

所以 HTTPS 的流程是：

```text
TCP 三次握手
  ↓
TLS 握手
  ↓
加密 HTTP 请求
  ↓
加密 HTTP 响应
```

---

# 7. HTTP 和 HTTPS 的结构区别

HTTP：

```text
HTTP 报文
  ↓
TCP
  ↓
IP
```

HTTPS：

```text
HTTP 报文
  ↓
TLS 加密
  ↓
TCP
  ↓
IP
```

注意：

> **HTTPS 里面的 HTTP 结构没有变，只是 HTTP 报文被 TLS 加密了。**

也就是说，HTTP 仍然是：

```text
请求行
请求头
空行
请求体
```

响应仍然是：

```text
状态行
响应头
空行
响应体
```

只是网络上传输时，别人看到的是加密后的 TLS 数据，而不是明文 HTTP。

---

# 8. 你可以这样理解

HTTP 像两个人用统一格式写信：

```text
第一行：我要干什么
后面几行：补充信息
空一行
正文：具体内容
```

TCP 像可靠邮递员：

```text
保证信件内容按顺序送到
```

HTTPS 像先把信装进加密信封：

```text
别人看不到，也改不了
```

---

# 9. 最终总结

你现在可以这样表达：

> TCP 把字节可靠地送到目标进程，但 TCP 不理解这些字节的含义。HTTP 是应用层协议，它规定了请求和响应的格式，包括请求行、请求头、请求体、状态行、响应头和响应体。客户端按照 HTTP 格式构造请求，服务端按照 HTTP 格式解析请求并返回响应，因此两个进程可以用统一语义进行通信。HTTPS 则是在 HTTP 和 TCP 之间加入 TLS，先建立安全通道，再加密传输 HTTP 报文。





